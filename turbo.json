{
	"$schema": "https://turborepo.com/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["build/**"]
		},
		"dev": {
			"cache": false,
			"persistent": true,
			"env": ["DATABASE_URL"],
			"inputs": ["$TURBO_DEFAULT$", ".env"]
		}
	}
}
